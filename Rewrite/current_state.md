# Current state

This file is the live handoff between sessions. Every session ends by editing this file. Keep it short and accurate.

## Active task

**T030 — STAR certificate PDF + trainee "My authorisations" panel.** Model: Opus.

Second Block 1 task. Produce a QuestPDF certificate for any `Active` entrustment decision, add the trainee's "My authorisations" dashboard panel (one card per active decision, download link), and an admin-facing revoke surface. See scope in `Rewrite/practical-plan.md` §T030. Existing `PortfolioPdfService` in `Wombat.Infrastructure/Reporting/` is the template to mirror.

## Last completed

**T029 — `EntrustmentDecision` aggregate.**

- Domain under `src/Wombat.Domain/EntrustmentDecisions/`: `EntrustmentDecision` (immutable, `Issue`/`Revoke`/`MarkExpired`/`SupersedeBy`/`Amend`-throws), `EntrustmentEvidenceLink`, `PendingEntrustmentDecision`, and the two enums.
- Application feature folder at `src/Wombat.Application/Features/EntrustmentDecisions/` with `IssueEntrustmentDecisionCommand`, `RevokeEntrustmentDecisionCommand`, `StagePendingEntrustmentDecisionCommand`, `RemovePendingEntrustmentDecisionCommand`, and the `GetActiveDecisionsForTrainee` / `GetDecisionHistoryForEpa` / `ListExpiringDecisions` / `ListPendingEntrustmentDecisionsForReview` queries. Authorisation helper parallels `CommitteeDecisionAuthorization`.
- Ratification integration: `RatifyCommitteeDecisionCommandHandler` now materialises all staged `PendingEntrustmentDecision` rows into `EntrustmentDecision` rows atomically on ratification, applies auto-supersession for prior `Active` decisions on the same `(TraineeUserId, EpaId)`, and clears the staging table in the same transaction.
- Daily `EntrustmentDecisionExpiryJob` at 03:30 UTC: flips past-expiry `Active` decisions to `Expired`, emails the trainee, and sends a 30-day expiring-soon reminder tracked via `LastExpiryReminderSentOn` (no duplicate reminders).
- EF configuration + hand-off migration `20260421104618_EntrustmentDecisions` (with `.Designer.cs` generated by `dotnet ef migrations add` — the CLI was reachable in this session). Three new tables: `EntrustmentDecisions`, `EntrustmentEvidenceLinks`, `PendingEntrustmentDecisions`. Indexes on `(TraineeUserId, EpaId, Status)` and `(Status, ExpiresOn)`. All FKs are `Restrict` on `EntrustmentLevels`, `Epas`, and `CommitteeReviews` (no cascade-deleting an authorisation); pending rows cascade from `CommitteeReviews` only.
- `ErasureExecutor` extended to pseudonymise `EntrustmentDecision.TraineeUserId`, `IssuedByChairUserId`, `RevokedByUserId`, and `PendingEntrustmentDecision.StagedByUserId` (all `private set` — raw SQL like the existing committee-review treatment). Retention reason `entrustment_decision` added.
- Minimal chair UI on `ReviewDetail.razor` — pending-decisions panel with EPA and level pickers (uses existing `ListEpasForSubSpecialityQuery` and `GetEntrustmentScalesListQuery`). Trainee/admin surfaces deferred to T030.
- Docs: `Rewrite/DOMAIN.md` updated — renamed "STAR reflection" section clarifies the acronym transition and adds the "Entrustment decision (STAR)" block.
- Tests: 16 new Domain tests (aggregate invariants, evidence-link rules, supersession, expiry/revocation happy and sad paths) + 6 new Application handler tests (chair authorisation, auto-supersession, atomic ratification-to-issue, admin-vs-chair revocation, queries). `ErasureCoverageTests` passes — new user-id columns are handled.

## Plan this session works against

`Rewrite/practical-plan.md` — the pragmatic post-rewrite plan. Four blocks, nine tasks (T028–T036), ~3–4 weeks total.

## Block 1 sequence

1. ✅ T028 — rename `star_reflection` → `reflective_note`
2. ✅ T029 — `EntrustmentDecision` aggregate
3. T030 — STAR certificate PDF + trainee "My authorisations" panel (active)

## Test status at handoff

- `dotnet build Wombat.sln -c Release` — zero errors, zero warnings
- Domain tests — 33/33 pass (16 new)
- Application tests — 128/128 pass (6 new)
- Architecture tests — 19/19 pass
- Web tests — 33/33 pass
- Infrastructure tests — 3/3 in isolation; the `SeedParseTests.AllSeedJsonFiles_ParseCleanly` test fails sporadically when the full solution runs in parallel — confirmed present on the pre-T029 baseline (`c517a3e`), so this is pre-existing file-sharing flakiness unrelated to T029.
- Integration tests — skipped locally (require Docker / Testcontainers per CLAUDE.md).

## Known compromises recorded in T029

- **bUnit smoke for `ReviewDetail.razor` pending panel not added.** ReviewDetail had no pre-existing bUnit tests and the pending panel is a single form + list — the render-safety we would assert is redundant against the application handler coverage. If the page gains a second bUnit test later, add a pending-panel smoke alongside it.
- **`IssuedByCommitteeReviewId` is `Restrict`-deleted.** If a committee review is ever deleted the foreign key blocks it. This is the intended behaviour for regulatory records; if an institution later needs review soft-deletion, revisit this FK.

## What remains (operational, not code — carried forward from T016)

- Execute `deploy/README.md` first-boot checklist against a real Linode server
- Configure DNS, TLS certificate (Caddy auto-provisions via ACME)
- Set production secrets in `/opt/wombat/config/wombat.env`
- Run `--seed` to provision the admin user and seeded activity types
- Revoke UPDATE/DELETE on AuditEntries table after first migration

## Companion reference docs

- `EPA Book/evaluation.md` — 92-requirement book scorecard (reference, not todo list)
- `EPA Book/critique.md` — literature-backed reasoning for practical-plan compromises
- `Rewrite/book-fidelity-plan.md` — superseded; kept only because `critique.md` cites it

## Last verified commits

- T029 commit — pending (this session will produce it)
- `dc506d1` — T028 (rename `star_reflection` → `reflective_note`)
- `bf583ee` — MailKit 4.16.0 security bump (closes GHSA-9j88-vvj5-vhgr)
- `864ad3b` — T016 (rewrite-complete baseline)
